<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>


    <%= csrf_meta_tags %>
  </head>

  <body>
    <div>
      <h1><%= link_to "BetaFish", stories_path %></h1>
      <% if logged_in? %>
        <p>Welcome back, <%=link_to current_user.username, user_path(current_user) %>
        <%=button_to 'Logout', logout_path, method: :delete %></p>
        <p><%= link_to "Publish", new_story_path %></p>
      <% else %>
        <p><%=link_to 'Login', login_path%>
        <%=link_to 'Register', new_user_path%></p>
      <% end %>
    </div>
    <%= yield %>

  </body>
</html>
